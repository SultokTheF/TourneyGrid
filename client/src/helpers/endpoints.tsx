const BASE_URL = "http://localhost:8000/api/v1";

const teams = `${BASE_URL}/teams/`;
const matches = `${BASE_URL}/matches/`;
const rounds = `${BASE_URL}/rounds/`;
const double_elimination_brackets = `${BASE_URL}/double_elimination_brackets/`;

export { teams, matches, rounds, double_elimination_brackets };
